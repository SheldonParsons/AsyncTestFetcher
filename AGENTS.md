# AsyncTest Fetcher

- Work directly in `/Users/sheldon/Documents/GithubProject/AsyncTestFetcher` on `main`. Do not create or use a separate worktree unless the user explicitly requests one. This is the authoritative development checkout; do not make further edits in old Codex worktrees.
- Chrome Manifest V3 extension, minimum Chrome 125. The user calls the native Side Panel “panel”.
- Current capture scope is a capability probe: page MAIN-world fetch/XHR response interception from the current authorized/bound active page, newest first, at most 20 in-memory rows across pages. Never clear the buffer on navigation, tab switches, listener teardown, or binding changes; only adding a new row beyond the limit evicts the oldest. History stays visible even when the current page cannot capture. Stop on leaving/navigation/unbinding/logout/hidden panel. Capture page-visible request/response headers, query params, full request URL up to the documented cap, and supported request/response bodies. Never claim browser-added/Cookie/Set-Cookie headers or multipart file bytes are fully captured. Request and response bodies are each bounded to 1 MiB, a 15-second body deadline and six concurrent readers per frame; label incomplete/unreadable bodies. No replay, upload, persistent capture store, DOM or screenshots. Continue origin + longest segment-aware prefix bindings and explicit choices for ambiguous projects.
- WXT + Vue 3 + TypeScript. Transitions.dev motion first, Reka UI for missing interaction primitives. Morphicons first, using Lucide icon data or static Lucide icons where appropriate. Black/white/gray UI; preserve the supplied brand logo.
- No Prettier requirement. Do not add frameworks or formatter infrastructure without a concrete need.
- Keep Chrome entries thin. UI belongs under `src/ui`, service configuration under `src/settings`. Do not prebuild recording, AI classification, or AsyncTest backend modules.
- Never fake a successful login or send credentials to a guessed endpoint. AsyncTest is the only backend; all API calls live in `src/api/asynctest` and run in the background. Optional host access is requested only through explicit service login or platform authorization. `scripting` and `webNavigation` support the response-capture probe. The user rejects the debugger infobar: do not use debugger. Inject only after checking authenticated user, platform authorization and selected project. Page bridge data is untrusted; never expose credentials to MAIN/ISOLATED capture scripts or accept privileged commands from them.
- Sessions persist in chrome.storage.local by the full API base URL. Per explicit user request, remembered credentials default on, save only after successful login, and remain after logout; unchecking immediately erases them for that service. Never save private_key. Only explicit authentication failure clears a session. Offline/5xx/permission errors preserve it as unverified. Logout is local-only: do not call the server's account-wide logout endpoint.
- Login success animation is fixed at 3000ms including settle/fade, triggered only by explicit successful login, with account content mounting underneath immediately. Restore must not replay. Welcome Logo hover reuses FetcherMark; preserve exact approved light spectrum stops, pure black bars, 0.24 glow and reduced-motion support.
- Test code belongs in `/Users/sheldon/Documents/AsyncTest/ast-testing-core`; browser profiles, databases, screenshots and reports belong in `/Users/sheldon/Documents/AsyncTest/ast-testing-core-data`. Never call an HTTP page test with mocked Chrome APIs a real extension acceptance test.
- Do not modify AsyncTest frontend/backend/configuration/knowledge. Do not spawn subagents without explicit authorization. Do not commit or push unless requested.
- The user now performs verification personally: do not run tests or browser QA unless explicitly requested. Rebuild the unpacked extension after code changes so the user can reload it.
- Use Node 22+, `npm ci`, and `npm run build`. Do not run tests, browser QA, or additional verification suites unless explicitly requested; the user reviews changes. Load `/Users/sheldon/Documents/GithubProject/AsyncTestFetcher/.output/chrome-mv3` as the unpacked extension. The dev runner is disabled so `npm run dev` does not launch a browser or install an extension automatically.
